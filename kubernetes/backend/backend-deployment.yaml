apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: hotel-backend
  labels:
    app: backend
    tier: backend

spec:
  replicas: 5
  selector:
    matchLabels:
      app: backend
      tier: backend
  
  template:
    metadata:
      labels:
        app: backend
        tier: backend

      annotations:
        # Use 'agent.openbao.org' (or 'vault.hashicorp.com' depending on your injector chart)
        agent.openbao.org/agent-inject: "true"
        agent.openbao.org/role: "hotel-backend"
        agent.openbao.org/agent-inject-secret-mongo-uri: "secret/data/hotel/backend"
        agent.openbao.org/agent-inject-template-mongo-uri: |     
          {{- with secret "secret/data/hotel/backend" -}}
          {{ .Data.data.MONGO_URI }}
          {{- end }}

    spec:
      serviceAccountName: backend

      containers:
        - name: backend
          image: host.docker.internal:8082/backend:8c283fbc6c4938d17ac732e9ce344f802b1cb0a2

          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
              name: http

          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret

          readinessProbe:
            httpGet:
              path: /hotels
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
          
          livenessProbe:
            httpGet:
              path: /hotels
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 20
          
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      
      imagePullSecrets:
        - name: regcred
