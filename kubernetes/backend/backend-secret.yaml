apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: hotel-backend
  labels:
    app: backend
    tier: backend
type: Opaque
stringData:
  DB_USER: admin
  DB_PASSWORD: password
  # MONGO_URI: "mongodb://admin:admin123@mongo.database.svc.cluster.local:27017/hotelapp?authSource=admin"
