apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: hotel-backend
  labels:
    app: backend
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: backend
    tier: backend
  ports:
    - name: http
      port: 3001
      targetPort: 3001