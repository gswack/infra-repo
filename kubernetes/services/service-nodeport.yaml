apiVersion: v1
kind: Service
metadata:
  name: backend-external
  namespace: hotel-backend
  labels:
    app: backend
    tier: backend
spec:
  type: NodePort
  selector:
    app: backend
    tier: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30300
