apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: hotel-backend
  labels:
    app: backend
    tier: backend
data:
  PORT: "3000"
  NODE_ENV: production
  MONGODB_URI: mongodb://mongodb.hotel-database.svc.cluster.local:27017/hotelapp
