apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: hotel-database
  labels:
    app: mongodb
    tier: database
spec:
  type: ClusterIP
  selector:
    app: mongodb
    tier: database
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
